# Production Environment Variables Template for Google Cloud Run
# Copy this file to .env.production and fill in your values

# ============================================
# SECRETS (Store in Google Secret Manager)
# ============================================
# These should NEVER be committed to git
# Use Google Secret Manager for production

# Anthropic API Configuration
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# JIRA Configuration
JIRA_API_TOKEN=your_jira_api_token_here
JIRA_EMAIL=your_jira_email@company.com
JIRA_SERVER=https://your-company.atlassian.net/
JIRA_JQL=PROJECT = "YOUR_PROJECT" AND type = epic

# Freshservice Configuration
FRESHSERVICE_DOMAIN=your-company.freshservice.com
FRESHSERVICE_API_KEY=your_freshservice_api_key_here

# ============================================
# ENVIRONMENT VARIABLES (Can be in Cloud Build)
# ============================================

# Azure AD Configuration (Build-time variables)
REACT_APP_AZURE_CLIENT_ID=your_azure_client_id_here
REACT_APP_AZURE_TENANT_ID=your_azure_tenant_id_here
REACT_APP_REDIRECT_URI=https://your-service-name.run.app

# Optional: LangChain Tracing (for debugging)
LANGCHAIN_TRACING_V2=false
LANGCHAIN_ENDPOINT=https://api.smith.langchain.com
LANGCHAIN_API_KEY=your_langchain_api_key_here
LANGCHAIN_PROJECT=excom-ai-production

# Optional: CORS Configuration
# Comma-separated list of allowed origins (if needed)
ALLOWED_ORIGINS=https://your-custom-domain.com

# ============================================
# GOOGLE CLOUD CONFIGURATION
# ============================================

# Your GCP Project ID
GCP_PROJECT_ID=your-gcp-project-id

# Cloud Run Region
GCP_REGION=us-central1

# Service Name
SERVICE_NAME=excom-ai

# ============================================
# HOW TO SET UP SECRETS IN GOOGLE CLOUD
# ============================================
#
# 1. Create secrets in Google Secret Manager:
#
#    echo -n 'your-secret-value' | gcloud secrets create secret-name --data-file=-
#
#    Example:
#    echo -n 'sk-ant-api03-xxxxx' | gcloud secrets create anthropic-api-key --data-file=-
#
# 2. Grant Cloud Run access to secrets:
#
#    gcloud secrets add-iam-policy-binding anthropic-api-key \
#      --member=serviceAccount:YOUR-PROJECT-NUMBER-compute@developer.gserviceaccount.com \
#      --role=roles/secretmanager.secretAccessor
#
# 3. Secrets are automatically loaded in Cloud Run via cloudbuild.yaml
#
# ============================================